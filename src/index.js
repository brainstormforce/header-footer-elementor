import React from 'react';
import ReactDOM from 'react-dom';

const SettingsPage = () => {
  return (
    <div>
      <h1>Welcome</h1>
      {/* Your settings components will go here */}
    </div>
  );
};

ReactDOM.render(<SettingsPage />, document.getElementById('settings-root'));
